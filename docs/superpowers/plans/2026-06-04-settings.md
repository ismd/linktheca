# Settings Screen Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Заменить заглушку `web/src/routes/settings.tsx` («Coming soon») на реальный read-only экран с двумя секциями-карточками: **Account** (идентичность из `useAuthStore`) и **About** (версия, режим, статус Radar из `useRadarStatusQuery`).

**Architecture:** Frontend-only, ноль изменений на бэке. Новый лёгкий модуль `features/settings/components/*` (без своих api/schemas — потребляет существующие `features/auth` store и `features/radar` хук). Версия выносится в общую константу `shared/version.ts` и переиспользуется в Sidebar (убираем дубль-хардкод). Маршрут `/settings` уже зарегистрирован в `App.tsx` — только переписываем тело.

**Tech Stack:** TypeScript, React, React Router v7, TanStack Query v5, Tailwind (editorial-классы проекта), Vitest + Testing Library + MSW.

**Spec:** `docs/superpowers/specs/2026-06-04-settings-design.md`

---

## Files touched

| Path | Изменение |
|---|---|
| `web/src/shared/version.ts` | create — `APP_VERSION` |
| `web/src/shared/layout/Sidebar.tsx` | modify — использовать `APP_VERSION` вместо хардкода |
| `web/src/features/settings/components/SettingRow.tsx` | create |
| `web/src/features/settings/components/SettingRow.test.tsx` | create |
| `web/src/features/settings/components/AccountSection.tsx` | create |
| `web/src/features/settings/components/AccountSection.test.tsx` | create |
| `web/src/features/settings/components/AboutSection.tsx` | create |
| `web/src/features/settings/components/AboutSection.test.tsx` | create |
| `web/src/routes/settings.tsx` | rewrite — header + две секции |
| `web/src/routes/settings.test.tsx` | create |

**Существующие факты, на которые опирается план (проверены):**
- `PageHeader` (`@/shared/layout/PageHeader`): props `{ title: string; subtitle?: string; actions?: ReactNode }`; рендерит `<h1 class="display-tight">{title}</h1>` + `<p>{subtitle}</p>`.
- `User` (`@/features/auth/store`): `{ id: number; email: string; displayName: string; isAdmin: boolean }`. `useAuthStore` — zustand; `getState().setSession(token, user)` / `clearSession()`.
- `useRadarStatusQuery` (`@/features/radar/use-radar`): `useQuery` → `{ data?: { lastSweepAt: Date | null }, isLoading, isSuccess, error, ... }`.
- `fmtSweep` (`@/features/radar/time`): `(d: Date | null) => string` → `"Last sweep · <relative>"` либо `"Awaiting first sweep"`.
- `ApiError` (`@/shared/api/errors`): поля `status: number`, `code: string`. `apiFetch` бросает `ApiError(res.status, code, …)`, где `code` берётся из JSON-тела `{ code }`.
- `radar_disabled`: бэкенд отдаёт его с HTTP `501` (`internal/radar/http.go`).
- MSW в тестах: `server` из `@/test/setup`, `onUnhandledRequest: "error"` → каждый запрос должен быть замокан. `apiFetch` бьёт по `/api/...`.
- Маршрут `/settings` уже в `App.tsx:45` (`{ path: "settings", element: <SettingsRoute /> }`) — менять `App.tsx` не нужно.
- Команды: тест одного файла — `npm test -- --run <path>`; весь набор — `npm test`; линт — `npm run lint`; типы — `npm run typecheck`; сборка — `npm run build`. Запускать из `web/`.

---

## Task 1: `shared/version.ts` + рефактор Sidebar

Версия сейчас захардкожена в `Sidebar.tsx` (`v0.1.0 · self-hosted`). Выносим в одну константу, чтобы Settings и Sidebar смотрели в один источник. Это модуль-константа + однострочная замена — отдельный unit-тест не пишем (нечего тестировать сверх типов); проверяем типами, существующим набором и сборкой.

**Files:**
- Create: `web/src/shared/version.ts`
- Modify: `web/src/shared/layout/Sidebar.tsx`

- [x] **Step 1: Создать `web/src/shared/version.ts`**

```ts
export const APP_VERSION = "0.1.0";
```

- [x] **Step 2: В `Sidebar.tsx` добавить импорт** (после строки `import { cn } from "@/shared/lib/cn";`)

```tsx
import { APP_VERSION } from "@/shared/version";
```

- [x] **Step 3: В `Sidebar.tsx` заменить захардкоженную строку версии**

Было:

```tsx
        <p className="label-sc text-muted-foreground">v0.1.0 · self-hosted</p>
```

Стало:

```tsx
        <p className="label-sc text-muted-foreground">v{APP_VERSION} · self-hosted</p>
```

- [x] **Step 4: Проверить типы и линт**

```bash
cd /home/ismd/coding/linktheca/web
npm run typecheck && npm run lint
```

Expected: без ошибок.

- [x] **Step 5: Commit**

```bash
git add web/src/shared/version.ts web/src/shared/layout/Sidebar.tsx
git commit -m "refactor(web): extract APP_VERSION constant, reuse in Sidebar"
```

---

## Task 2: `SettingRow` (презентационная строка)

**Files:**
- Create: `web/src/features/settings/components/SettingRow.tsx`
- Test: `web/src/features/settings/components/SettingRow.test.tsx`

- [x] **Step 1: Написать падающий тест `SettingRow.test.tsx`**

```tsx
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import { SettingRow } from "./SettingRow";

describe("SettingRow", () => {
  it("renders label and value", () => {
    render(<SettingRow label="Email" value="claude@ismd.dev" />);
    expect(screen.getByText("Email")).toBeInTheDocument();
    expect(screen.getByText("claude@ismd.dev")).toBeInTheDocument();
  });
});
```

- [x] **Step 2: Запустить тест — убедиться, что падает**

```bash
cd /home/ismd/coding/linktheca/web
npm test -- --run src/features/settings/components/SettingRow.test.tsx
```

Expected: FAIL (модуль не найден).

- [x] **Step 3: Реализовать `SettingRow.tsx`**

```tsx
type Props = {
  label: string;
  value: React.ReactNode;
};

export function SettingRow({ label, value }: Props) {
  return (
    <div className="flex items-center justify-between gap-6">
      <div className="label-sc text-muted-foreground">{label}</div>
      <div className="font-body text-ink text-right">{value}</div>
    </div>
  );
}
```

- [x] **Step 4: Запустить тест — убедиться, что проходит**

```bash
npm test -- --run src/features/settings/components/SettingRow.test.tsx
```

Expected: PASS (1 тест).

- [x] **Step 5: Commit**

```bash
git add web/src/features/settings/components/SettingRow.tsx web/src/features/settings/components/SettingRow.test.tsx
git commit -m "feat(settings/web): add SettingRow presentational component"
```

---

## Task 3: `AccountSection`

**Files:**
- Create: `web/src/features/settings/components/AccountSection.tsx`
- Test: `web/src/features/settings/components/AccountSection.test.tsx`

- [x] **Step 1: Написать падающий тест `AccountSection.test.tsx`**

```tsx
import { describe, it, expect, beforeEach } from "vitest";
import { render, screen } from "@testing-library/react";
import { useAuthStore } from "@/features/auth/store";
import { AccountSection } from "./AccountSection";

beforeEach(() => {
  useAuthStore.getState().clearSession();
});

describe("AccountSection", () => {
  it("renders name, email, and Member role for a non-admin user", () => {
    useAuthStore.getState().setSession("tok", {
      id: 1, email: "claude@ismd.dev", displayName: "ismd", isAdmin: false,
    });
    render(<AccountSection />);
    expect(screen.getByText("ismd")).toBeInTheDocument();
    expect(screen.getByText("claude@ismd.dev")).toBeInTheDocument();
    expect(screen.getByText("Member")).toBeInTheDocument();
  });

  it("renders Administrator role for an admin user", () => {
    useAuthStore.getState().setSession("tok", {
      id: 2, email: "admin@ismd.dev", displayName: "Admin", isAdmin: true,
    });
    render(<AccountSection />);
    expect(screen.getByText("Administrator")).toBeInTheDocument();
  });

  it("renders nothing when there is no user", () => {
    const { container } = render(<AccountSection />);
    expect(container).toBeEmptyDOMElement();
  });
});
```

- [x] **Step 2: Запустить тест — убедиться, что падает**

```bash
cd /home/ismd/coding/linktheca/web
npm test -- --run src/features/settings/components/AccountSection.test.tsx
```

Expected: FAIL (модуль не найден).

- [x] **Step 3: Реализовать `AccountSection.tsx`**

```tsx
import { useAuthStore } from "@/features/auth/store";
import { SettingRow } from "./SettingRow";

export function AccountSection() {
  const user = useAuthStore((s) => s.user);
  if (!user) return null;

  const initial = user.displayName.charAt(0).toUpperCase() || "·";
  const role = user.isAdmin ? "Administrator" : "Member";

  return (
    <section className="px-4 lg:px-8 py-8 border-b border-rule">
      <div className="mb-6">
        <h2 className="display-tight text-2xl text-ink mb-1">Account</h2>
        <p className="font-body italic text-sm text-muted-foreground">
          Who you are to this instance.
        </p>
      </div>
      <div className="bg-paper-2 border border-rule p-6 md:p-8 flex flex-col gap-6">
        <div className="flex items-center gap-5">
          <div className="w-16 h-16 bg-ink text-paper flex items-center justify-center font-mono text-xl">
            {initial}
          </div>
          <div className="display-tight text-2xl text-ink">{user.displayName}</div>
        </div>
        <div className="flex flex-col gap-4 border-t border-rule pt-6">
          <SettingRow label="Email" value={user.email} />
          <SettingRow label="Role" value={role} />
        </div>
      </div>
    </section>
  );
}
```

- [x] **Step 4: Запустить тест — убедиться, что проходит**

```bash
npm test -- --run src/features/settings/components/AccountSection.test.tsx
```

Expected: PASS (3 теста).

- [x] **Step 5: Commit**

```bash
git add web/src/features/settings/components/AccountSection.tsx web/src/features/settings/components/AccountSection.test.tsx
git commit -m "feat(settings/web): add AccountSection (read-only identity)"
```

---

## Task 4: `AboutSection`

**Files:**
- Create: `web/src/features/settings/components/AboutSection.tsx`
- Test: `web/src/features/settings/components/AboutSection.test.tsx`

- [ ] **Step 1: Написать падающий тест `AboutSection.test.tsx`**

```tsx
import { describe, it, expect, beforeEach } from "vitest";
import { render, screen, waitFor } from "@testing-library/react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { http, HttpResponse } from "msw";
import { server } from "@/test/setup";
import { useAuthStore } from "@/features/auth/store";
import { AboutSection } from "./AboutSection";

function wrapper() {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  const Wrapper = ({ children }: { children: React.ReactNode }) => (
    <QueryClientProvider client={qc}>{children}</QueryClientProvider>
  );
  Wrapper.displayName = "TestWrapper";
  return Wrapper;
}

beforeEach(() => {
  useAuthStore.getState().setSession("access", {
    id: 1, email: "u@x.co", displayName: "U", isAdmin: false,
  });
});

describe("AboutSection", () => {
  it("shows version, mode, and the radar sweep line when radar is enabled", async () => {
    server.use(
      http.get("/api/radar/status", () =>
        HttpResponse.json({ last_sweep_at: null })),
    );
    render(<AboutSection />, { wrapper: wrapper() });
    expect(screen.getByText("v0.1.0")).toBeInTheDocument();
    expect(screen.getByText("self-hosted")).toBeInTheDocument();
    await waitFor(() =>
      expect(screen.getByText("Awaiting first sweep")).toBeInTheDocument(),
    );
  });

  it("shows Disabled when radar is turned off", async () => {
    server.use(
      http.get("/api/radar/status", () =>
        HttpResponse.json(
          { code: "radar_disabled", message: "radar feature is disabled on this server" },
          { status: 501 },
        )),
    );
    render(<AboutSection />, { wrapper: wrapper() });
    await waitFor(() =>
      expect(screen.getByText("Disabled")).toBeInTheDocument(),
    );
  });
});
```

- [x] **Step 2: Запустить тест — убедиться, что падает**

```bash
cd /home/ismd/coding/linktheca/web
npm test -- --run src/features/settings/components/AboutSection.test.tsx
```

Expected: FAIL (модуль не найден).

- [x] **Step 3: Реализовать `AboutSection.tsx`**

```tsx
import { ApiError } from "@/shared/api/errors";
import { useRadarStatusQuery } from "@/features/radar/use-radar";
import { fmtSweep } from "@/features/radar/time";
import { APP_VERSION } from "@/shared/version";
import { SettingRow } from "./SettingRow";

function radarStatusLabel(status: ReturnType<typeof useRadarStatusQuery>): string {
  if (status.isLoading) return "Checking…";
  if (status.isSuccess) return fmtSweep(status.data.lastSweepAt);
  if (status.error instanceof ApiError && status.error.code === "radar_disabled") {
    return "Disabled";
  }
  return "Unavailable";
}

export function AboutSection() {
  const status = useRadarStatusQuery();

  return (
    <section className="px-4 lg:px-8 py-8">
      <div className="mb-6">
        <h2 className="display-tight text-2xl text-ink mb-1">About</h2>
        <p className="font-body italic text-sm text-muted-foreground">
          This instance.
        </p>
      </div>
      <div className="bg-paper-2 border border-rule p-6 md:p-8 flex flex-col gap-4">
        <SettingRow label="Version" value={`v${APP_VERSION}`} />
        <SettingRow label="Mode" value="self-hosted" />
        <SettingRow label="Radar" value={radarStatusLabel(status)} />
      </div>
    </section>
  );
}
```

- [x] **Step 4: Запустить тест — убедиться, что проходит**

```bash
npm test -- --run src/features/settings/components/AboutSection.test.tsx
```

Expected: PASS (2 теста).

- [x] **Step 5: Commit**

```bash
git add web/src/features/settings/components/AboutSection.tsx web/src/features/settings/components/AboutSection.test.tsx
git commit -m "feat(settings/web): add AboutSection with radar status row"
```

---

## Task 5: Переписать маршрут `settings.tsx` + тест

**Files:**
- Modify (rewrite): `web/src/routes/settings.tsx`
- Test: `web/src/routes/settings.test.tsx`

- [x] **Step 1: Написать падающий тест `settings.test.tsx`**

```tsx
import { describe, it, expect, beforeEach } from "vitest";
import { render, screen } from "@testing-library/react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { http, HttpResponse } from "msw";
import { server } from "@/test/setup";
import { useAuthStore } from "@/features/auth/store";
import SettingsRoute from "./settings";

function wrapper() {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  const Wrapper = ({ children }: { children: React.ReactNode }) => (
    <QueryClientProvider client={qc}>{children}</QueryClientProvider>
  );
  Wrapper.displayName = "TestWrapper";
  return Wrapper;
}

beforeEach(() => {
  useAuthStore.getState().setSession("access", {
    id: 1, email: "u@x.co", displayName: "U", isAdmin: false,
  });
  server.use(
    http.get("/api/radar/status", () => HttpResponse.json({ last_sweep_at: null })),
  );
});

describe("SettingsRoute", () => {
  it("renders the Settings, Account, and About headings", () => {
    render(<SettingsRoute />, { wrapper: wrapper() });
    expect(screen.getByRole("heading", { name: "Settings" })).toBeInTheDocument();
    expect(screen.getByRole("heading", { name: "Account" })).toBeInTheDocument();
    expect(screen.getByRole("heading", { name: "About" })).toBeInTheDocument();
  });
});
```

- [x] **Step 2: Запустить тест — убедиться, что падает**

```bash
cd /home/ismd/coding/linktheca/web
npm test -- --run src/routes/settings.test.tsx
```

Expected: FAIL — тест рендерит старую заглушку, заголовков `Account`/`About` нет.

- [x] **Step 3: Переписать `web/src/routes/settings.tsx`**

Полностью заменить содержимое файла на:

```tsx
import { PageHeader } from "@/shared/layout/PageHeader";
import { AccountSection } from "@/features/settings/components/AccountSection";
import { AboutSection } from "@/features/settings/components/AboutSection";

export default function SettingsRoute() {
  return (
    <div>
      <PageHeader title="Settings" subtitle="This instance and your account." />
      <AccountSection />
      <AboutSection />
    </div>
  );
}
```

- [x] **Step 4: Запустить тест — убедиться, что проходит**

```bash
npm test -- --run src/routes/settings.test.tsx
```

Expected: PASS (1 тест).

- [x] **Step 5: Commit**

```bash
git add web/src/routes/settings.tsx web/src/routes/settings.test.tsx
git commit -m "feat(settings/web): wire Settings route to Account + About sections"
```

---

## Task 6: Финальная проверка

**Files:** —

- [ ] **Step 1: Полный набор тестов фронта**

```bash
cd /home/ismd/coding/linktheca/web
npm test
```

Expected: ALL PASS (включая новые settings-тесты, без регрессий).

- [ ] **Step 2: Типы + линт + сборка**

```bash
npm run typecheck && npm run lint && npm run build
```

Expected: без ошибок и warnings (`lint` падает на любом warning из-за `--max-warnings 0`).

- [ ] **Step 3: Ручной smoke в браузере**

Поднять dev-окружение (Go-сервер + `npm run dev`, залогиниться). Затем:

1. Открыть `/settings` → большой заголовок «Settings» + подзаголовок. Секция **Account**: аватар-инициал, display name, Email, Role (`Member` или `Administrator`).
2. Секция **About**: `Version v0.1.0`, `Mode self-hosted`, `Radar` со строкой `Last sweep · …` (или `Awaiting first sweep`, если sweep'ов ещё не было).
3. Перезапустить сервер с `LINTHECA_RADAR_ENABLED=false` (или эквивалентным флагом отключения Radar) → на `/settings` строка `Radar` показывает `Disabled`; остальной экран рендерится нормально.
4. Проверить футер сайдбара: по-прежнему `v0.1.0 · self-hosted` (значение из `APP_VERSION`).

- [ ] **Step 4: Финальный коммит (если smoke выявил правки — зафиксировать; иначе пропустить)**

```bash
git add -A
git commit -m "chore(settings/web): manual smoke fixes"
```

Фича готова к ревью.

---

## Self-review

**Spec coverage:**

| Секция спеки | Покрыта задачей |
|---|---|
| `shared/version.ts` + рефактор Sidebar | Task 1 |
| `SettingRow` | Task 2 |
| `AccountSection` (identity, role, read-only) | Task 3 |
| `AboutSection` (version, mode, radar) | Task 4 |
| Маппинг состояния Radar (loading/success/disabled/error) | Task 4 (`radarStatusLabel`) |
| Композиция маршрута `settings.tsx` | Task 5 |
| Тесты (Account variants, About enabled/disabled, route headings) | Tasks 3–5 |
| Явные не-цели (no backend, без edit/tokens/appearance/notifications) | Не вводятся ни в одной задаче |

Все секции спеки имеют задачу. Backend не затрагивается. `App.tsx` не меняется (маршрут уже зарегистрирован).

**Placeholder scan:** плейсхолдеров нет — в каждом шаге реальный код, точные пути, команды и ожидаемый результат.

**Type/naming consistency:** `APP_VERSION`, `SettingRow({label,value})`, `AccountSection`, `AboutSection`, `radarStatusLabel`, `useRadarStatusQuery().data.lastSweepAt`, `fmtSweep`, `ApiError.code` использованы одинаково во всех задачах и совпадают с проверенными сигнатурами существующего кода. Импорт-пути (`@/features/auth/store`, `@/features/radar/use-radar`, `@/features/radar/time`, `@/shared/api/errors`, `@/shared/version`, `@/shared/layout/PageHeader`) согласованы. Тестовые фикстуры Radar используют `last_sweep_at` (snake_case, как ждёт `RawRadarStatusSchema`) и HTTP `501` для `radar_disabled` (как на бэке).
